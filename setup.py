from setuptools import setup, find_packages

setup(
    name="fastapi_async_models",
    version="0.1.0",
    description="Paquete que incluye una aplicación API REST hecha con FastAPI.",
    packages=find_packages(),
    package_data={'readme':['README.md']}
)